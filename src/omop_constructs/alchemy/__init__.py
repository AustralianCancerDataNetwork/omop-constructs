"""
High-level construct families built on top of ``omop-alchemy`` models.

The subpackages under :mod:`omop_constructs.alchemy` expose the repository's
active analytical constructs, mostly as ORM-mapped materialized views plus the
query fragments used to build them.
"""
